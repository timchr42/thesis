<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name="de.cispa.byetrack.TokenReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="org.mozilla.geckoview.CAPABILITY_TOKENS" />
            </intent-filter>
        </receiver>


        <!-- and ends with
        <service
        android:name="de.cispa.byetrack.CapabilityService"
        android:exported="true" />
         -->
    </application>
</manifest>